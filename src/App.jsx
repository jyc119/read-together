import { BrowserRouter, Routes, Route } from "react-router-dom";
import React from "react";

const App = () => {
  return (
    <BrowserRouter>
      <div className="relative z-0 text-9xl bg-primary">App</div>
    </BrowserRouter>
  );
};

export default App;
